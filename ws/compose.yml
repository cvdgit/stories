services:
    wikids-ws:
        build:
            context: .
            dockerfile: ./Dockerfile
        volumes:
            - .:/app
            - '${CERT_PATH}:/app/cert'
        working_dir: /app
        restart: unless-stopped
        ports:
            - "4000:4000"
