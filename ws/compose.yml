version: "3.5"
services:
    wikidsws:
        container_name: "wikidsws"
        restart: "always"
        build:
            context: .
            dockerfile: ./Dockerfile
        volumes:
            - .:/app
            - '${CERT_PATH}:/app/cert'
        working_dir: /app
        ports:
            - "4000:4000"
